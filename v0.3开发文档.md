# 华为 eNSP 配置批量生成工具 · v0.3 开发文档

> 定位：**个人运维提效工具**，不是商业软件。目标只有一个——把已验证过的配置命令（`../华为ensp基础配置命令.md`）固化成模板，勾选参数即出脚本，复制即贴进 eNSP。
>
> 三条纪律：**能用一个函数解决的不写一个类；能用标准库解决的不装第三方包；生成函数必须纯净（不碰界面）。**

---

## 1. 技术栈与运行

| 项 | 选择 | 说明 |
|---|---|---|
| 语言 | Python 3.11+ | `.python-version` 固定版本 |
| GUI | tkinter + ttk（标准库） | 零依赖，PyInstaller 可打单文件 exe |
| 数据存储 | JSON（variables.json） | IP/VLAN 库 + 用户数据，不进 git |
| IP 校验 | ipaddress（标准库） | 输错弹窗提示，不崩溃 |
| 打包 | `pyinstaller -F -w main.py`（唯一第三方包） | frozen 模式下 variables.json 存 exe 目录 |

日常命令：`uv run python main.py`（或 `python main.py`）。**运行期零第三方依赖**，拷个文件夹就能跑。

---

## 2. 目录结构（v0.3 现状）

```
demo-v0.1/
├── main.py               # 入口：只启动主窗口
├── gui.py                # 主窗口 App：左侧 IP/VLAN 库面板 + 设备类别切换 + 页签 + 底部输出区
│                         #   以及滚轮统一路由 _on_wheel
├── variables.py          # 变量库读写（variables.json；frozen 模式存 exe 目录）
├── variables.json        # 用户运行数据（gitignore，不进库）
├── pyproject.toml / uv.lock / .python-version   # 运行环境
├── v0.3开发文档.md        # 本文档
├── widgets/              # 与业务无关的共享界面控件
│   ├── port_field.py     #   PortField：类型下拉(GE/ETH) + 编号输入，get() 出完整接口名
│   ├── rows_editor.py    #   RowsEditor：通用"添加/删一行"编辑器，列类型见下文
│   └── scroll_frame.py   #   ScrollFrame：竖向滚动容器（页面外层统一包裹）
├── pages/                # 界面页，按设备类别分文件
│   ├── __init__.py       #   CATEGORY_PAGES 注册表：类别 -> 页签顺序
│   ├── switch.py         #   交换机：接口VLAN / 三层网关 / VRRP / MSTP / 链路聚合
│   ├── dhcp_switch.py    #   交换机 DHCP（Vlanif 接口）
│   ├── dhcp_router.py    #   路由器 DHCP（物理接口）
│   ├── interface.py      #   路由器接口IP配置（行式编辑）
│   └── common.py         #   双栖页面：OSPF / ACL（交换机、路由器共用）
└── modules/              # 纯生成函数（不 import tkinter）
    ├── __init__.py       #   说明性 docstring
    ├── base.py           #   PORT_TYPE_MAP 注册表、port_name()、collect_vlans()、render_vlan_batch()
    ├── if_vlan.py        #   接口 access/trunk（文档 1.1~1.3）
    ├── vlanif.py         #   Vlanif 三层网关（2.1）
    ├── vrrp.py           #   VRRP 双机，generate_device / generate_pair（2.2）
    ├── mstp.py           #   MSTP 域 + 实例根桥（2.3）
    ├── eth_trunk.py      #   Eth-Trunk 链路聚合（2.4）
    ├── ospf.py           #   OSPF network 宣告（2.5）
    ├── acl.py            #   普通/高级 ACL + traffic-filter 绑定（2.7）
    ├── dhcp_switch.py    #   DHCP global/relay（2.6）
    ├── dhcp_router.py    #   DHCP global/relay（4.11）
    └── router_if.py      #   路由器接口 IP（4.1）
```

---

## 3. 架构分层

```
main.py → gui.App
             ├── LibraryPanel（左侧 IP/VLAN 库，增删即存即刷新）
             ├── 设备类别下拉（交换机/路由器）→ 按 CATEGORY_PAGES 重建页签
             ├── 页签 = pages/ 里的页面类实例（每个类别各自装配，实例常驻，切走再切回不丢表单）
             │      页面 collect/validate → 调 modules/ 生成函数 → 填底部预览区
             └── 底部输出区：生成当前模块 / 汇总生成全部 / 复制 / 保存 .cfg / 清空
```

- **modules 层零界面依赖**：想验证某个模块，命令行 `print(module.generate({...}))` 一秒对拍 md 文档。
- **页面按设备类别组织**：`pages/__init__.py` 的 `CATEGORY_PAGES` 决定每个类别显示哪些页签；双栖页面（DHCP/OSPF/ACL）两个类别共享同一实例。

### 3.1 页面契约（pages/ 每个页类固定实现）

```python
TITLE                            # 页签标题
collect()  -> (params, errors)   # 收集表单值
validate(params) -> errors       # 校验（返回错误文案列表）
render(params) -> str            # 单页生成（可含 vlan batch 头）
render_summary(params) -> str    # 汇总用正文（vlan batch 由 App 统一放头部）
summary_vlans(params) -> set     # 本页用到的 VLAN，供汇总头部去重
set_lib_values(ip_list, vlan_list)  # 库变化时刷新本页下拉选项
enabled (tk.BooleanVar)          # 是否参与汇总（"参与汇总"勾选框）
is_empty(params) -> bool（可选）  # 汇总时跳过空页
OWN_SCROLL = True（可选）        # 页面自带滚动区时，App 不再包外层 ScrollFrame
```

生成按钮固定流程：`collect → validate → generate → 预览区`，有错误弹窗拦截。

### 3.2 模块 = 纯生成函数

`generate(params) -> str`，params 进配置字符串出，块与块之间用 `#\n` 分隔（与 md 文档格式一致）。

---

## 4. 模块清单（v0.3 全部 11 个生成模块）

| 模块 | 页面（设备） | 文档章节 | 页面输入 | 生成要点 |
|---|---|---|---|---|
| 接口VLAN | 接口VLAN（交换机） | 1.1~1.3 | 勾选行：类型/编号/access/trunk + VLAN/PVID/allow-pass | access 出 `port default vlan`，trunk 出 pvid + allow-pass；模式切换自动置灰无关列 |
| 三层网关 | 三层网关（交换机） | 2.1 | 每行 VLAN ID + IP + 掩码 | `interface VlanifN` + `ip address` |
| VRRP | VRRP（交换机） | 2.2 | 每行 VLAN/掩码/虚拟IP/设备1 IP+角色/设备2 IP+角色 + 通用参数 | 按行独立主备角色；"生成设备1/2/一键两台"三按钮；汇总时只并入选定一侧 |
| MSTP | MSTP（交换机） | 2.3 | 区域名 + 每行 实例/VLAN/根桥角色 | `stp region-configuration` 域配置 + `stp instance N root primary/secondary` |
| 链路聚合 | 链路聚合（交换机） | 2.4 | Eth-Trunk 编号 + 放行 VLAN 行 + 成员接口行（**类型 GE/ETH 下拉** + 编号） | trunk 口 + `mode lacp-static`（参数里带）+ 成员口 `eth-trunk N` |
| DHCP | DHCP（交换机） | 2.6 | 模式 global/relay + Vlanif + 网关/网段/掩码/DNS/租期（中继则填服务器IP） | `ip pool pool-vlanN` 全局池 或 接口中继；池名 `pool-vlan{VLAN}` |
| DHCP | DHCP（路由器） | 4.11 | 同上但接口为物理口（PortField） | 池名取网段倒数第二段；绑定物理接口 |
| 接口IP配置 | 接口IP配置（路由器） | 4.1 | 每行 **接口类型(GE/ETH 下拉) + 序号 + IP(IP库下拉) + 子网掩码 + 描述(可选)** | `interface GE0/0/0` + `description`（可选）+ `ip address`；掩码兼容 `/24`、`24`、点分三种写法 |
| OSPF | OSPF（双栖） | 2.5/4.2 | 进程号/Router-ID/Area + 每行 network/反掩码 | `ospf N router-id X` + area + network 宣告 |
| ACL | ACL（双栖） | 2.7 | 类型 basic/advanced、编号、每行规则、可选绑定接口+方向 | basic 只出 source；advanced 出 source+destination；绑接口时追加 `traffic-filter` |

---

## 5. 关键机制（改代码前必读）

### 5.1 接口类型注册表与接口名拼接（modules/base.py）

```python
PORT_TYPE_MAP = {"GE": "GigabitEthernet", "ETH": "Ethernet"}
```

- **加新类型（如 XGE）只改这一个映射**：`PortField` 控件、接口VLAN 页、链路聚合页、路由器接口页的下拉全部自动带出，生成函数零改动。
- `port_name(type, num)`：编号含 `/`（如 `1/0/1`、`0/0/22`）原样使用；纯数字自动补 `0/0/`（`1` → `GigabitEthernet0/0/1`）。全项目的接口名都走这一个函数。

### 5.2 vlan batch 自动汇总

手写脚本最容易漏 `vlan batch`。两条路径都会自动扫 VLAN、去重排序、放头部：

- **单页生成**：各模块的 `generate_full()` = `render_vlan_batch(collect_vlans(...))` + 正文。
- **汇总生成全部**（gui.App.generate_all）：遍历当前类别中勾了"参与汇总"的页面，`render_summary` 拼正文（`#\n` 分隔），VLAN 取各页 `summary_vlans()` 的并集，只在顶部出一次。VRRP 页特殊：汇总时只并入"汇总设备"下拉选中的一侧。

### 5.3 IP / VLAN 库联动

- `variables.py` 读写 `variables.json`（`{"ip": [...], "vlan": [...]}`），损坏/缺失返回空库不抛异常。
- 库增删后 `App._sync_lib` 调所有页面的 `set_lib_values(ip, vlan)`，全页面下拉实时刷新。
- IP/VLAN 输入框都是**可编辑下拉**：下拉选 `192.168.10.0` 后直接改末位 `.1`，加快连续配置。

### 5.4 widgets 共享控件

| 控件 | 用途 |
|---|---|
| `PortField` | 类型下拉 + 编号输入，`get()` 返回完整接口名；编号留空返回空串（可选接口留空即跳过） |
| `RowsEditor` | "添加一行/删一行"编辑器。列类型：`None` 普通输入框；`"ip"`/`"vlan"` 库下拉；`(a,b)` 固定选项只读下拉；`"port"` PortField；`"check"` 勾选框。VRRP/MSTP/OSPF/ACL/链路聚合在用 |
| `ScrollFrame` | 竖向滚动容器。gui 装配页面时默认给每页外面包一层；页面内容放进 `.inner` |

### 5.5 页面滚动与滚轮路由（v0.3.1）

- 除接口VLAN页（`OWN_SCROLL = True`，工具条/表头固定、行区自滚）外，**所有页面外层包 ScrollFrame**，行多时可上下滚动。
- 滚轮**不在各控件里各自绑定**（多页面会互相抢 bind_all）：`App._on_wheel` 在 App 级统一 `bind_all("<MouseWheel>")`，按指针位置向上找最近的 `tk.Canvas` 滚动；预览 Text、库面板 Treeview 走各自原生滚动；下拉弹层等其他 toplevel 不接管。

### 5.6 VRRP 双机的特殊性

- 每行 VLAN 规划独立设置设备1/设备2 的主/备角色；主角色接口才生成 priority/preempt/track。
- 三个生成入口：生成设备1 / 生成设备2 / 一键两台（预览用，两段分开标注，**不能整份贴同一台设备**）。
- `App.generate_vrrp_device(target)` 是 VRRP 专用按钮逻辑，不走通用生成流程。

---

## 6. 新增一个模块/页面的三步

1. `modules/` 新建 `xxx.py`，写纯函数 `generate(params) -> str`（docstring 里写 params 格式示例；需要 vlan 头就加 `collect_vlans` + `generate_full`）。
2. `pages/` 对应类别文件里写页类，实现 3.1 的契约；双栖页面放 `common.py`。
3. `pages/__init__.py` 的 `CATEGORY_PAGES` 对应类别加一项。完。

不需要注册 modules（pages 直接 import），不需要改 gui.py。

---

## 7. 边界（刻意不做的事）

- **不做配置下发**：只生成脚本，不连设备。
- **不做配置解析**：不解析已有配置，只从零生成。
- **不做严格校验**：VLAN 范围、IP/掩码匹配关系等只做基础检查（数字、合法 IP），跳过空行。
- **验证方式是对拍**：生成结果与 md 文档标准块逐行比对（历史上由 check.py 承担，已清理；需要时按模块契约临时写脚本跑 `generate` 即可）。

---

## 8. v0.3 功能清单（相对 v0.2 的累积变更）

**新增**
- DHCP 双端支持：交换机（Vlanif）/路由器（物理口）两套页面与生成模块，global + relay 两模式
- 路由器接口IP配置页：行式编辑（类型下拉/序号/IP/掩码/可选描述），对应文档 4.1
- 链路聚合成员接口类型 GE/ETH 下拉选择
- 百兆口（ETH）支持：接口类型注册表扩展，全页面生效
- 页面按设备类别（交换机/路由器）组织页签
- 全部页面支持上下滚动（ScrollFrame + 滚轮统一路由）

**修复与工程化**
- 修复 VRRP 参数键名（`device1_ip/device2_ip`）；汇总角色选择避免双机配置混入一份脚本
- 修复基础聚合缺 `mode lacp-static`
- `variables.py` 打包后存 exe 目录，避免退出丢库
- 清理仓库：.gitignore 排除 `__pycache__`/`variables.json`；删除历史测试脚本与阶段性报告
